# 制作过程记录草稿

## 一、选题背景

本作品主题为“国际中文教育资源制作”，选择 HSK 写作训练作为具体方向。许多国际中文学习者在写作时需要了解自己的作文是否切题、结构是否清楚、词汇和语法是否符合当前水平。因此，本作品设计了一个 HSK 写作智能批改网页，帮助学习者进行自我练习，也帮助教师收集学习者写作语料。

## 二、学习对象

本资源面向 HSK 1-6 级国际中文学习者。学习者可以根据自己的 HSK 等级选择对应题目，输入中文作文，查看评分、批改建议和下一步训练任务。

## 三、教学目标

1. 语言知识目标：帮助学习者掌握不同 HSK 等级写作中常见的词汇、句式和连接表达。
2. 写作能力目标：引导学习者围绕题目完成内容表达，增强篇章结构和逻辑连贯意识。
3. 反馈改进目标：通过评分维度和批改建议，让学习者了解作文中的优点和问题。
4. 教师辅助目标：通过本地语料记录，为教师分析学习者写作问题提供材料。

## 四、网页结构设计

网页整体借鉴在线写作批改工具的界面形式，采用“题目卡片 + 作文输入 + 右侧分数 + 批改报告 + 语料后台”的结构。

主要区域包括：

1. 题目区：选择 HSK 等级和题目类型，显示建议用时、建议字数和写作要求。
2. 作文区：学习者输入作文，系统统计字数和句子数。
3. 评分区：显示综合分和五个维度分数。
4. 批改报告区：显示划线标注、诊断建议、推荐表达和训练任务。
5. 语料后台：保存作文记录，支持搜索、筛选、详情查看、回填、删除、导出 CSV、导出 JSON 和导入 JSON。
6. 作品说明：说明作品的教学性、技术性、创新性和部署方式。

## 五、评分维度设计

本作品采用教学模拟评分，不代表官方 HSK 成绩。评分维度包括：

1. 内容切题：作文是否回应题目，是否包含主题关键词。
2. 结构连贯：句子数量、段落组织和连接词使用情况。
3. 词汇等级：是否使用与当前 HSK 等级相匹配的词汇和表达。
4. 语法表达：句子长度、重复输入、英文混入等问题。
5. 汉字标点：中文比例、标点使用和文本规范性。

## 六、技术实现

网页使用纯 HTML、CSS、JavaScript 制作。

HTML 用于搭建页面结构，包括题目、输入框、评分卡、报告区、表格后台和作品说明。

CSS 用于设计页面视觉，包括浅灰背景、白色卡片、蓝色分数、响应式布局、按钮、表格和深色模式。

JavaScript 用于实现核心功能，包括：

1. HSK 等级和题目切换。
2. 作文字数、句子数统计。
3. 教学模拟评分算法。
4. 批改报告生成。
5. 文本划线标注。
6. 连词助手。
7. 示例作文填入。
8. 本地语料保存。
9. 语料表格渲染。
10. 语料搜索、等级筛选、分数筛选和问题标签筛选。
11. 语料详情查看和单条记录删除。
12. CSV 导出、JSON 导出和 JSON 导入。
13. 深浅色模式切换。

## 七、语料后台说明

由于 GitHub Pages 是静态网页托管平台，无法直接提供服务器数据库。本作品使用浏览器本地存储 localStorage 记录用户作文。每次评分后，如果勾选“保存到本地语料后台”，系统会保存作文时间、学习者匿名编号、班级或批次、HSK 等级、题目、字数、句子数、综合分、维度分、问题标签、作文原文和批改报告。

后台可以根据关键词、HSK 等级、分数区间和问题标签筛选语料，也可以查看单条作文详情。导出的 CSV 适合表格分析，导出的 JSON 适合后续系统迁移或二次开发。

这种方式适合课程作业演示和个人练习。如果后续需要真实多人在线语料收集，可以扩展连接 Supabase、Firebase 或学校服务器。扩展思路是：前端页面保持不变，在评分后把记录提交到数据库；教师端从数据库读取全班数据，生成等级分布、错误类型分布和学习进步曲线。

## 八、创新点

1. 将 HSK 写作训练和自动批改界面结合。
2. 支持不同 HSK 等级的写作任务切换。
3. 通过五个维度呈现可解释的评分结果。
4. 使用划线标注模拟作文批改效果。
5. 将学习者作文保存为本地语料，并支持筛选、详情查看和删除。
6. 支持 CSV 和 JSON 两种导出形式，便于教师分析和后续扩展。
7. 网页既能本地打开，也能部署到 GitHub Pages 在线访问。

## 九、AI 辅助过程

制作过程中使用 AI 辅助完成选题构思、页面结构设计、HSK 等级任务设计、评分维度设计、网页代码生成、样式调整、交互功能实现和问题排查。AI 生成内容经过人工筛选和修改，最终形成符合课程要求的网页资源。

## 十、遇到的问题与解决方法

问题一：GitHub Pages 不能直接保存所有用户数据。  
解决方法：使用 localStorage 实现本地语料后台，并在作品说明中说明其局限和扩展方案。

问题二：纯静态网页无法真正调用大型语言模型完成深度批改。  
解决方法：采用可解释的规则化教学模拟评分，重点展示教育技术设计思路和前端交互能力。

问题三：评分结果需要让学习者看懂。  
解决方法：把总分拆分为内容、结构、词汇、语法、标点五个维度，并给出文字建议。

## 十一、演示视频建议流程

1. 打开网页，介绍作品主题和功能。
2. 选择一个 HSK 等级和写作题目。
3. 点击示例作文或手动输入作文。
4. 演示字数和句子数统计。
5. 点击“开始评分”，展示综合分和维度分。
6. 展示批改报告、划线标注、推荐表达和训练建议。
7. 打开语料后台，展示记录保存、统计卡片、搜索筛选、问题标签和详情查看。
8. 演示语料回填、CSV 导出、JSON 导出和 JSON 导入。
9. 展示作品说明，解释本地打开、GitHub Pages 部署和真实后台扩展方案。

## 十二、总结反思

通过本次网页制作，我认识到现代教育技术不仅是网页制作技术，更重要的是将教学目标、学习反馈和技术功能结合起来。本作品尝试用简单稳定的前端技术完成一个可用的国际中文写作训练资源，同时也体现了数据收集和学习分析的初步思路。
